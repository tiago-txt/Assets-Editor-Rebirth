<Window x:Class="Assets_Editor.MigrationMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Migrations (old server → Canary)"
        Width="420" Height="320" WindowStartupLocation="CenterOwner"
        Style="{StaticResource MaterialDesignWindow}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Choose a migration to run:" FontSize="14" Margin="0,0,0,12"/>
        <ListBox x:Name="MigrationList" Grid.Row="1" BorderThickness="1" Padding="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="4,6">
                        <materialDesign:PackIcon Kind="DatabaseExport" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <StackPanel>
                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Description}" FontSize="11" Foreground="{DynamicResource MaterialDesignBodyLight}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Run selected" Click="RunSelected_Click" Style="{StaticResource MaterialDesignRaisedButton}" Padding="14,6" Margin="0,0,8,0"/>
            <Button Content="Close" Click="Close_Click" Style="{StaticResource MaterialDesignFlatButton}" Padding="14,6"/>
        </StackPanel>
    </Grid>
</Window>
